require 'bot/adapter'

describe Bot::Adapter do
  skip 'loads settings from JSON' do
  end

  skip 'keeps track of registered plugins' do
    # Hooks, auth, read every line etc.
  end

  skip 'handles exceptions' do
  end

  skip 'passes message to core' do
  end
end
