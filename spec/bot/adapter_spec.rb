require 'bot/adapter'

describe Bot::Adapter do
  pending 'loads settings from JSON' do
  end

  pending 'keeps track of registered plugins' do
    # Hooks, auth, read every line etc.
  end

  pending 'handles exceptions' do
  end

  pending 'passes message to core' do
  end
end