require 'spec_helper'
require 'bot/core'
require 'bot/adapter'
require 'adapters/irc/irc'

describe Dummy do
  it 'connects to an IRC server' do
  end

  it 'connects to an SSL IRC server' do
  end

  it 'connects to an SSL IRC server with an invalid certificate' do
  end

  it 'parses PRIVMSG' do
  end

  it 'starts a ping timeout check on connection' do
  end
end